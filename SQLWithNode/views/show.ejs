<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/style.css">
    <title>FAMILY</title>
</head>
<body>
    <h1>There are <%= users.length %> no. of users who's trust Us</h1>
    <a href="/user/sign">SignUp</a>
    <br><br>
    <br>
    <% for(let user of users) { %>
        <ul>
            <li><%= user.id %></li>
            <li><%= user.name %></li>
            <li><%= user.email %></li>
            <a href="/user/edit/<%= user.id %>">edit</a>
        </ul>
    <% } %>
</body>
</html>